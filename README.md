<!--  birthday_flower js 적용하기 -->
* user_birthday > 클릭 시 12로 변경을 해야해서 onclick 입력
* document > 현재 열려있는 웹페이지
* user_birthday > 찾고자 하는 대상 (id)
* querySelector > 돋보기
* const > 담아둔다 (변수저장)
* p와 p가 형제일 때 두번째 p만 크기가 커서 변경을 해야한다면 p+p로 선택자 지정가능
* `복습` line-height > 행간
* js에서 console~function 까지 등록하고 html에 각각 입력했으나 버튼을 눌러도 색도, 팝업창도 안 뜸
    > 중간중간 저장을 안 눌러서 일어남
* main에 position: relative를 주는 이유 `기준점 만들기`
* 팝업에 position: absolute를 주는 이유 `원래 있던 자리에서 빠져나와 위에 떠있게 하기`
* 팝업 배경에 position: absolute를 주는 이유 `main 전체를 덮는 검은 배경을 만들려고`